<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mensaje Especial</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background-color: #000;
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(76, 0, 130, 0.15) 0%, transparent 20%),
                radial-gradient(circle at 90% 80%, rgba(76, 0, 130, 0.15) 0%, transparent 20%);
            padding: 20px;
            text-align: center;
        }
        
        .container {
            max-width: 800px;
            width: 90%;
            padding: 40px;
            background-color: rgba(20, 20, 20, 0.85);
            border-radius: 20px;
            box-shadow: 0 0 30px rgba(138, 43, 226, 0.4);
            border: 1px solid rgba(138, 43, 226, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .container::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(138, 43, 226, 0.1) 0%, transparent 70%);
            z-index: -1;
            animation: rotate 20s linear infinite;
        }
        
        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .header {
            margin-bottom: 40px;
            position: relative;
        }
        
        h1 {
            color: #8A2BE2;
            font-size: 3.5rem;
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 15px;
            text-shadow: 0 0 15px rgba(138, 43, 226, 0.7);
            animation: pulse 3s infinite;
        }
        
        @keyframes pulse {
            0% { text-shadow: 0 0 15px rgba(138, 43, 226, 0.7); }
            50% { text-shadow: 0 0 25px rgba(138, 43, 226, 1), 0 0 40px rgba(138, 43, 226, 0.8); }
            100% { text-shadow: 0 0 15px rgba(138, 43, 226, 0.7); }
        }
        
        .divider {
            height: 3px;
            width: 80%;
            margin: 0 auto;
            background: linear-gradient(90deg, transparent, #8A2BE2, transparent);
        }
        
        .message {
            font-size: 1.5rem;
            line-height: 1.8;
            margin: 30px 0;
            color: #fff;
            padding: 0 20px;
            position: relative;
        }
        
        .heart {
            color: #ff4d94;
            display: inline-block;
            font-size: 2rem;
            animation: heartbeat 1.2s infinite;
        }
        
        @keyframes heartbeat {
            0% { transform: scale(1); }
            10% { transform: scale(1.2); }
            20% { transform: scale(1); }
            30% { transform: scale(1.2); }
            40% { transform: scale(1); }
        }
        
        .stars {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }
        
        .star {
            position: absolute;
            background-color: #fff;
            border-radius: 50%;
            animation: twinkle var(--duration, 3s) infinite ease-in-out;
        }
        
        @keyframes twinkle {
            0%, 100% { opacity: 0.2; }
            50% { opacity: 1; }
        }
        
        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            .message { font-size: 1.2rem; }
        }
        
        @media (max-width: 480px) {
            h1 { font-size: 2rem; }
            .message { font-size: 1rem; }
            .container { padding: 20px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="stars" id="stars"></div>
        
        <div class="header">
            <h1>Hola corason</h1>
            <div class="divider"></div>
        </div>
        
        <div class="message">
            <p>Esto es una prueba de que se puede hacer asique no se si funcionara. SI funciona dimelo porque voy a hacer mas cositas chulas asi. Te amo <span class="heart">❤</span></p>
        </div>
    </div>

    <script>
        // Crear estrellas para el fondo
        const starsContainer = document.getElementById('stars');
        const starsCount = 150;
        
        for (let i = 0; i < starsCount; i++) {
            const star = document.createElement('div');
            star.classList.add('star');
            
            // Posición aleatoria
            const posX = Math.random() * 100;
            const posY = Math.random() * 100;
            star.style.left = `${posX}%`;
            star.style.top = `${posY}%`;
            
            // Tamaño aleatorio
            const size = Math.random() * 3;
            star.style.width = `${size}px`;
            star.style.height = `${size}px`;
            
            // Duración aleatoria para la animación
            const duration = 2 + Math.random() * 3;
            star.style.setProperty('--duration', `${duration}s`);
            
            // Retraso inicial aleatorio
            const delay = Math.random() * 5;
            star.style.animationDelay = `${delay}s`;
            
            starsContainer.appendChild(star);
        }
    </script>
</body>
</html>
